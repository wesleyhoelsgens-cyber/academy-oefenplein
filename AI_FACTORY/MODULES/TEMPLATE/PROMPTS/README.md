# Prompt placeholders

Add prompts only when the module needs AI interaction. Name them `01_ACTION_NAME.md`,
`02_ACTION_NAME.md`, and so on. Every prompt must contain Purpose, When to use, Variables, Prompt,
Example and Expected output. Read project-specific values from `../CONFIG.md`.
